services:
  - type: web
    name: user-registration-system
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm run install:backend
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
